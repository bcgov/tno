import styled from 'styled-components';

export const ClipDirectoryTable = styled.div`
  .download {
    cursor: pointer;
    color: darkblue;
    padding-left: 20px;
  }

  .delete {
    cursor: pointer;
    color: red;
    padding-left: 20px;
  }

  .attach {
    cursor: pointer;
    color: darkblue;
    padding-left: 20px;
  }

  .hidden {
    display: none;
  }

  .ft-row {
    padding-top: 4px;
  }
`;
