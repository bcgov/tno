import styled from 'styled-components';
import { FormPage } from 'tno-core';

export const MediaTypeForm = styled(FormPage)`
  display: flex;
  flex-direction: column;
  align-items: center;

  .back-button {
    align-self: start;
  }
`;
