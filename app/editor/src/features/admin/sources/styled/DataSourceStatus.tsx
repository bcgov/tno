import { Col } from 'components/flex';
import styled from 'styled-components';

export const DataSourceStatus = styled(Col)`
  input[name$='startAt'],
  input[name$='stopAt'] {
    width: 115px;
  }
`;
