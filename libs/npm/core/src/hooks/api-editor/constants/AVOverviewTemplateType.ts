export enum AVOverviewTemplateType {
  /// <summary>
  /// A template related to a weekday for the evening overview.
  /// </summary>
  Weekday = 0,
  /// <summary>
  /// A template related to a weekend for the evening overview.
  /// </summary>
  Weekend = 1,
}
