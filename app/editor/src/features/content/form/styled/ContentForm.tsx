import styled from 'styled-components';

export const ContentForm = styled.div`
  #txa-summary {
    height: 6.25em;
  }

  .checkbox-column {
    margin-left: 2em;

    & > div:first-child {
      max-height: 12em;
    }
  }

  .content-properties {
    padding-top: 1em;
  }

  .licenses {
    padding-left: 2em;
  }
`;
