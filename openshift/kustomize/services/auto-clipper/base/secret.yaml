kind: Secret
apiVersion: v1
metadata:
  name: azure-speech-services
  namespace: default
  annotations:
    description: Azure Speech Services secrets
    created-by: jeremy.foster
  labels:
    name: azure-speech-services
    part-of: tno
    version: 1.0.0
    component: transcription
    managed-by: kustomize
type: Opaque
stringData:
  AZURE_REGION: ${AZURE_REGION}
  AZURE_SPEECH_SERVICES_KEY: ${AZURE_SPEECH_SERVICES_KEY}
---
kind: Secret
apiVersion: v1
metadata:
  name: azure-openai
  namespace: default
  annotations:
    description: Azure Cognitive Services secrets
    created-by: jeremy.foster
  labels:
    name: azure-openai
    part-of: tno
    version: 1.0.0
    component: ai
    managed-by: kustomize
type: Opaque
stringData:
  AZURE_OPENAI_KEY: ${AZURE_OPENAI_KEY}
