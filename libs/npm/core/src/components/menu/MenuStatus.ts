/**
 * The navigation menu status.
 */
export enum MenuStatus {
  /**
   * The navigation menu is currently hidden.
   */
  hidden = 'hidden',
  /**
   * The navigation menu is narrow and only displays icons or abbreviations.
   */
  narrow = 'narrow',
  /**
   * The menu is fully expanded.
   */
  full = 'full',
}
