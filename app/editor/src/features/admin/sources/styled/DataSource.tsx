import { FormPage } from 'components/form/formpage';
import styled from 'styled-components';

export const DataSource = styled(FormPage)`
  .back-button {
    margin-bottom: 2em;
  }
  .form-actions {
    padding: 0.5em;
  }
`;
